{
	"name": "monorepo",
	"description": "Carbon monorepo",
	"private": true,
	"scripts": {
		"build": "turbo run build",
		"cf": "pnpm build --filter @buape/carbon... && turbo run dev --filter cloudo",
		"dev": "dotenv -- turbo run dev --filter !cloudo",
		"lint": "biome check . --apply",
		"preinstall": "npx only-allow pnpm",
		"start": "dotenv -- turbo run start",
		"test": "vitest",
		"generate": "turbo gen create-package",
		"web": "turbo run web:start --filter website",
		"web:build": "turbo run web:build --filter website"
	},
	"dependencies": {
		"@turbo/gen": "2.0.14",
		"dotenv-cli": "7.4.2",
		"dotenv-mono": "1.3.14",
		"tsc-watch": "6.2.0",
		"turbo": "2.0.14",
		"typedoc": "0.26.6",
		"vitest": "2.0.5"
	},
	"devDependencies": {
		"@biomejs/biome": "1.8.3",
		"typescript": "5.4.2"
	},
	"packageManager": "pnpm@9.3.0"
}
