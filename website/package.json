{
	"name": "website",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"docs:api": "node scripts/generate-typedoc.mjs",
		"dev": "pnpm run docs:api && tome dev",
		"build": "pnpm run docs:api && tome build",
		"deploy": "pnpm run build && tome deploy"
	},
	"devDependencies": {
		"@tomehq/cli": "0.3.3",
		"@tomehq/theme": "0.3.3",
		"react": "19.2.3",
		"react-dom": "19.2.3",
		"vite": "6.4.1"
	}
}
