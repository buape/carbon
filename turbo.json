{
	"$schema": "https://turbo.build/schema.json",
	"globalEnv": ["GITHUB_SERVER_URL"],
	"tasks": {
		"build": {
			"outputs": ["dist/**"],
			"cache": true,
			"dependsOn": ["^build"]
		},
		"test": {},
		"start": {
			"dependsOn": ["build"]
		},
		"dev": {
			"persistent": true
		}
	}
}
